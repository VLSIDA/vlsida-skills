apiVersion: v1
kind: Pod
metadata:
  # Pod name must start with the submitter's CruzID (e.g., jdoe-dev-shell)
  # so cluster admins and other users can identify the owner at a glance.
  name: __CRUZID__-__POD_NAME__
  namespace: __NAMESPACE__
  labels:
    app: __APP_LABEL__
    user: __CRUZID__
spec:
  containers:
  - name: workspace
    image: ubuntu:24.04
    command: ["sleep", "infinity"]
    volumeMounts:
    - name: data
      mountPath: /data
    resources:
      requests:
        memory: 8Gi
        cpu: 4
      limits:
        memory: 16Gi
        cpu: 8
    # Uncomment for GPU access:
    # resources:
    #   requests:
    #     nvidia.com/gpu: 1
    #   limits:
    #     nvidia.com/gpu: 1
  volumes:
  - name: data
    persistentVolumeClaim:
      claimName: __PVC_NAME__
  restartPolicy: Never
